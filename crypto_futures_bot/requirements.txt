# Minimal bağımlılık — indikatörler saf pandas/numpy ile hesaplanır (ta-lib YOK).
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
# Canlı fiyat akışı (WebSocket) — küçük-hedef scalping'de hızlı çıkış/trailing için.
websocket-client>=1.6.0
